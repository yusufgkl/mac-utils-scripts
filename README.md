# mac-utils-scripts
macOS utils scripts

** mac cleanup sh
from [HERE](https://github.com/mac-cleanup/mac-cleanup-sh)

** fdupes

`brew install fdupes`
`fdupes -rdN `
1. -r = recursive
2. -d = delete
3. -N = preserve first, delete the rest
